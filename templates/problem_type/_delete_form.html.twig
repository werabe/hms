<form method="post" action="{{ path('problem_type_delete', {'id': problem_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ problem_type.id) }}">
    <button class="btn">Delete</button>
</form>
