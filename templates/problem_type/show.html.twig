{% extends 'base.html.twig' %}

{% block title %}ProblemType{% endblock %}

{% block body %}
    <h1>ProblemType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ problem_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ problem_type.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ problem_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('problem_type_index') }}">back to list</a>

    <a href="{{ path('problem_type_edit', {'id': problem_type.id}) }}">edit</a>

    {{ include('problem_type/_delete_form.html.twig') }}
{% endblock %}
