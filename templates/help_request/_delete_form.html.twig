<form method="post" action="{{ path('help_request_delete', {'id': help_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ help_request.id) }}">
    <button class="btn">Delete</button>
</form>
